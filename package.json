{
  "name": "nova-install-bot",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "Telegram bot that installs the Nova panel on a user's own Cloudflare account.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.116.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.111.0"
  }
}
